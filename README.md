# Website for Coffea Consulting

# Dependencies

* ruby, 2+
* aws commandline tools

# Building/Deploying

Run `deploy.sh` if you've run `aws configure --profile shawn` to completion. CircleCI will build and
deploy anything pushed to the master branch.
